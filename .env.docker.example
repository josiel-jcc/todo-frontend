# Used by docker compose (and also injected into the frontend build).
VITE_API_URL=https://api-todo.infoos.shop

# Cloudflare Tunnel token (create a Tunnel in Cloudflare Zero Trust)
# Never commit the real token.
CLOUDFLARE_TUNNEL_TOKEN=REPLACE_ME

# Optional: host port for local access
FRONTEND_PORT=8080

